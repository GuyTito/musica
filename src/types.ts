export interface IconProps {
  className?: string
}

export type RepeatOptions = 'ONE' | 'ENABLED' | 'DISABLED'

export interface SongType {
  artist: string
  title: string
  audio: string
  duration: string
  cover: string
}